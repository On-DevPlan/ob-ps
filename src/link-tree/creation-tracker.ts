/**
 * creation-tracker.ts — empty placeholder.
 *
 * Both `capture()` (removed in bklink topic feature) and `buildDedupSet()`
 * (orphaned when snapshot-hook.ts was deleted in Task 7) are gone.
 * This file is kept only so other modules' relative import paths
 * and future tooling references still resolve. Add new helpers here
 * when the link-tree pipeline needs them.
 */
export {};
